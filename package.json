{
  "name": "card-game",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "docker compose up",
    "dev:frontend": "npm run dev --prefix client/app",
    "dev:backend:detach": "docker compose up -d",
    "down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
